[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT --workers 4 --timeout-keep-alive 120"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
numReplicas = 1
restartPolicyType = "on_failure"
